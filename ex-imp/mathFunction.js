let add = (a, b) => a + b;
let sub = (a, b) => a - b;
let div = (a, b) => a / b;
let mul = (a, b) => a * b;

// to export single function
//export default add;

// to export multiple function
export { add, sub, div, mul };
